# Duplicate this to .env

# Storage Configuration
# Set to 'local' for localStorage (client-side only) or 'remote' for PostgreSQL database
NEXT_PUBLIC_STORAGE_TYPE='local' ## 'local' or 'remote'

# Database Configuration (only required if NEXT_PUBLIC_STORAGE_TYPE='remote')
DATABASE_URL='postgresql://user:password@host:5432/database' ## PostgreSQL connection string - create free database: https://neon.com/

# App Configuration
APP_URL='http://localhost:3000' ## Your app URL
GOOGLE_SITE_VERIFICATION_ID='' ## Optional - for Google Search Console